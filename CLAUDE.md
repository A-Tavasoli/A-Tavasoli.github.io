# Project

This repository is the user's personal homepage, hosted on GitHub Pages at `A-Tavasoli.github.io`.

## Workflow

- After every update, run `git push` so the changes are published to the live page.
- Always confirm with the user before pushing.
